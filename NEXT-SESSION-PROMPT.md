# Промпт для следующей сессии

Скопируй в новую сессию:

---

Продолжаем «Школу 911» — банк заданий ФИПИ на Railway, ветка `fipi-importer`.
Сначала прочитай `SESSION-HANDOFF-2026-08-21.md` в корне и авто-память (особенно
`ege-fipi-bank`, `fipi-answer-keys`, `prod-without-guardrails`).

Коротко где мы: ОГЭ добит (часть 1 — 91,7%). ЕГЭ базовая полностью добавлена в
платформу — 7953 задачи импортированы, видны через переключатель ОГЭ/ЕГЭ, есть
админка сортировки `/admin/sort`, автопроверяемо ~18% (ключи набиваются
`tools/fipi/solve_ege_base.py` против оракула ЕГЭ).

**Первым делом:** проверь, добежал ли фоновый перебор ключей ЕГЭ
(`solve_ege_base.py --probe`) — если да, применить новые ключи к проду
(команда в конце HANDOFF, `apply-answers.mjs` по guid). Затем свериться, сколько
стало ключей ЕГЭ, и обновить память.

**Приоритет — «добить ключи ЕГЭ»** (решение владельца): перебор в границах +
подумать про внешние кандидаты для ЕГЭ (`hints.py` пока только ОГЭ). Остальные
~82% базовой требуют новых решателей/читателей чертежей.

**Дальше по продукту:** разборы (ФИПИ не публикует, чужие копировать нельзя —
генерировать ИИ+вычитка в `/admin/solutions`; ноль пока — часть 2 ОГЭ и ЕГЭ);
профильная математика ЕГЭ как второй трек (GUID `AC437B34557F88EA4115D2F374B0A07B`).

Важные грабли: деплой импортёра ДО прод-запуска; хранилище — локальный диск, так
что импорт с картинками идёт на контейнере; `railway ssh` моргает — с ретраями;
tRPC вход — superjson `{"json":{…}}`; ФИПИ доступен только с VPN владельца.

Владелец гонит правки прямо в прод (пусто, живых учеников нет), но перед
операцией, переписывающей задачи, — бэкап `pnpm export:bank`. Аддитивное
(новый трек, ключи) — можно напрямую.

---

## Открытые вопросы к владельцу (обсудить в начале)
- Стратегия: контент (задачи/ответы/разборы) не уникален — он есть на 4ege,
  ctege, РешуЕГЭ/sdamgia, Alexlarin. Наш ров — продукт (сортировка по номеру,
  авторитетные ключи, чистая автопроверка, прогресс), не контент. Согласны ли
  вкладываться в продукт/разборы, а не гнаться за «всеми разборами»?
- Перебор ключей ЕГЭ грузит `ege.fipi.ru` — какая задержка комфортна и до какого
  покрытия гнать (диминишинг-ретёрны после детерминированного прохода)?
- Сортировка `unsorted` (1170) — редактор сам через `/admin/sort` или прицельно
  расширять сигнатуры классификатора?

# Отчёт о работе · 19–21 августа 2026

Проект **«Школа 911»** — банк заданий ФИПИ на Railway.
Ветка `fipi-importer`. Живой сайт: https://school-911-production.up.railway.app
Полный контекст и грабли — в авто-памяти (`ege-fipi-bank`, `fipi-answer-keys`,
`fipi-bank-protocol`, `shkola-911-infra`, `shkola-911-project`,
`prod-without-guardrails`, `authed-e2e-testing`, `keep-answer-entry-simple`).

## Снимок состояния (прод)

| | Значение |
|---|---|
| Задач ОГЭ | 3884, часть 1 автопроверяема на **91,7%** (2786 ключей) |
| Задач ЕГЭ базовой | **7953** (трек `ege-mathematics`), автопроверяемо **~1435 (18%)** |
| Всего ключей в базе | ~4220 (2786 ОГЭ + ~1435 ЕГЭ) |
| Картинок ЕГЭ в галерее | 2960 |
| Коммитов за сессию | 47 (13 не запушены на момент отчёта) |

## Что сделано

### Фаза 1 — добивание ОГЭ (19.08)
- **Формулы-картинки**: правило «span = формула» ловило 541 из 966; переписано на
  3 признака → 137 условий и 54 общих текста починены на проде.
- **Ключи части 1**: 2586 → 2786 (шины/форматы листов/№10/№12 — 127/127 без
  отказов; №18 через новый читатель чертежа — линия сетки больше не выдаёт себя за
  сторону; перебор в границах `probe` — 47/65).
- **Экран ввода ответа** `/admin/answers`: очередь задач без ключа, пишет только
  `correctAnswer`. Проверка вынесена в `shared/answerValidation.ts`.
- **Сквозной тест под учеником** (Playwright, живой прод): нашёл и починил 2 бага —
  карточка не помнила решение (`publicBank.getTask` теперь отдаёт попытку) и тег
  аналитики стрелял 502 (`%VITE_ANALYTICS_ENDPOINT%`).

### Фаза 2 — ЕГЭ базовая (20–21.08)
Полностью добавлена в платформу:
- **Импортёр развёрнут в профиль банка**: `--host`/`--track`/`--source-title`,
  читает готовый `kim_number`. ОГЭ-дефолты целы.
- **Крауль** банка базовой: 7953 задачи (`ege.fipi.ru`,
  `proj=E040A72A1A3DABA14C90C97E0B6EE7DC`) + 3104 картинки.
- **Спецификация базовой 2026** в репо (`research/fipi-ege-2026/`), структура КИМ
  1–21 (`tools/fipi/fipi/ege_base.py`).
- **Классификатор** (`tools/fipi/fipi/classify_ege_base.py`): 85% — номер, где
  уверен, + именованные корзины для неразрешимых семей (1/14/16, 9/10/12, 11/13,
  1/2, 4/20/21 — как 23/25 в ОГЭ), 15% `unsorted`. У базового КЭС наполовину нет,
  номер читается из формулировки.
- **Импорт в прод**: 7953 задачи + 2960 картинок в трек `ege-mathematics`,
  27 типов заданий (сид `tools/db/seed-ege-base-types.mjs`).
- **Мультитрек-роутинг**: `getTrackBySlug` (дефолт ОГЭ), `listTasks`/`overview`
  берут `trackSlug`, `getTask` по slug без трека; `TaskBank` — переключатель
  ОГЭ/ЕГЭ. Проверено на проде.
- **Админка сортировки** `/admin/sort`: `sortQueue`/`assignNumber` (track-aware),
  редактор присваивает номер `unsorted`/корзинам, меняя только `examTaskTypeId`.
- **Ключи**: `tools/fipi/solve_ege_base.py` (solve-then-verify против оракула
  ЕГЭ, устойчив к TLS-блипам). Детерминированный проход — **1420** (потолок ~1421).

## Что идёт прямо сейчас (в фоне)

**Перебор в границах для ЕГЭ** — `solve_ege_base.py --probe --delay 0.6`,
581 задача-кандидат, пишет в `tools/fipi/out-ege-base/answers.jsonl` (резюмируемо).
На момент отчёта ~1435 ключей и растёт. **Как закончится — применить к проду:**
```bash
# дедупнуть и залить
python3 -c "import json;d={};[d.__setitem__(json.loads(l)['guid'],l) for l in open('tools/fipi/out-ege-base/answers.jsonl') if l.strip()];open('/tmp/a.jsonl','w').writelines(d.values())"
railway volume files --volume school-911-volume upload --overwrite /tmp/a.jsonl /import/ege-answers.jsonl
railway ssh -- node /data/import/apply-answers.mjs /data/import/ege-answers.jsonl
```
(`apply-answers.mjs` матчит по `sourceRecordId`=guid, трек-agnostic, идемпотентно.)

## Что осталось (приоритет — по решению владельца «добить ключи ЕГЭ»)

1. **Ключи ЕГЭ, добор** — перебор (идёт) + внешние кандидаты. `hints.py` заточен
   под ОГЭ-каталог; для ЕГЭ нужен свой источник. Остальные ~82% базовой —
   текстовые/геометрия/чтение графиков, нужны новые решатели/читатели (как №18).
2. **Разборы** — ФИПИ не публикует, чужие копировать нельзя (права). Путь —
   черновик ИИ (условие+ключ→решение) + вычитка в `/admin/solutions`. Ноль пока:
   часть 2 ОГЭ (909), часть 2 ЕГЭ (вся). Профильная математика ЕГЭ — отдельный
   трек, GUID `AC437B34557F88EA4115D2F374B0A07B` (в памяти `ege-fipi-bank`).
3. **Сортировка `unsorted`** (1170 + корзины) — редакторская работа через
   `/admin/sort`; движок готов.

## Операционные грабли (важно!)

- **Деплой ДО прод-запуска импортёра.** Прод-контейнер крутит задеплоенный
  `server/importFipi.ts`; коммит без деплоя = старый код (это дало залёт 20 задач
  ЕГЭ в ОГЭ-трек). После `git commit` в импортёр — `railway up`, потом запуск.
- **3 класса коллизий импорта** (все решены): старый импортёр без `--track`;
  internalId с префиксом OGE; неуникальный short_id → ЕГЭ slug/internalId несут
  первые 8 hex guid.
- **Хранилище — локальный диск `/data/storage`** (не S3). Импортёр с картинками
  идёт НА контейнере; данные доставляются тарболом на том и распаковываются.
- **`railway ssh` калечит составные команды** и моргает верификацией ключа —
  многошаговое класть файлом на том, гонять с ретраями. Иногда `upload` не
  перезаписывает при сбое API → давать свежие имена файлам.
- **tRPC вход — superjson**: `?input={"json":{...}}`, ответ `result.data.json.…`.
- **`oge.fipi.ru`/`ege.fipi.ru` режут по стране выхода IP** — краулить/солвить с
  машины владельца через VPN (не с Railway).

## Инструменты на томе (`/data/import/`)
`import-ege.sh`/`launch-full.sh` (детачед импорт + лог `full-import.log`),
`clean-ege-track.mjs` (снести ЕГЭ-трек), `seed-ege-base-types.mjs`,
`apply-answers.mjs`, `apply-statements.mjs`. Тарбол: `/import/ege-base.tar.gz`.

import { useAuth } from "@/_core/hooks/useAuth";
import { PlatformHeader } from "@/components/PlatformHeader";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { FilePlus2, Loader2, Pencil, Plus, ShieldCheck } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";

function slugify(value: string) { return value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "new-task"; }
type Status = "draft" | "review" | "published";
type Difficulty = "basic" | "standard" | "advanced";
type AnswerKind = "short_integer" | "short_decimal" | "short_text" | "manual";
const panel = "theme-surface rounded-[24px] border p-5 sm:p-6";
const orangeButton = "rounded-xl bg-[#ff5b14] font-extrabold text-[#101014] hover:bg-[#ff7a35]";

export default function AdminTasks() {
  const { user, loading } = useAuth();
  const isAdmin = user?.role === "admin";
  const options = trpc.school.admin.options.useQuery(undefined, { enabled: isAdmin });
  const tasks = trpc.school.admin.tasks.useQuery(undefined, { enabled: isAdmin });
  const [editId, setEditId] = useState<number | null>(null);
  const selectedTask = trpc.school.admin.getTask.useQuery({ taskId: editId ?? 0 }, { enabled: isAdmin && Boolean(editId) });
  const defaultTopic = useMemo(() => options.data?.topics[0]?.slug ?? "", [options.data]);
  const defaultKim = useMemo(() => options.data?.taskTypes[0]?.kimNumber ?? "", [options.data]);
  const [title, setTitle] = useState(""); const [slug, setSlug] = useState(""); const [statement, setStatement] = useState(""); const [solution, setSolution] = useState(""); const [correctAnswer, setCorrectAnswer] = useState(""); const [topic, setTopic] = useState(""); const [kim, setKim] = useState(""); const [status, setStatus] = useState<Status>("draft"); const [difficulty, setDifficulty] = useState<Difficulty>("standard"); const [answerKind, setAnswerKind] = useState<AnswerKind>("short_integer");
  const clearForm = () => { setEditId(null); setTitle(""); setSlug(""); setStatement(""); setSolution(""); setCorrectAnswer(""); setTopic(""); setKim(""); setStatus("draft"); setDifficulty("standard"); setAnswerKind("short_integer"); };
  useEffect(() => { if (!selectedTask.data) return; setTitle(selectedTask.data.title); setSlug(selectedTask.data.slug); setStatement(selectedTask.data.statementMarkdown); setSolution(selectedTask.data.solutionMarkdown); setCorrectAnswer(selectedTask.data.correctAnswer ?? ""); setTopic(selectedTask.data.topicSlug ?? ""); setKim(selectedTask.data.kimNumber); setStatus(selectedTask.data.status === "archived" ? "draft" : selectedTask.data.status); setDifficulty(selectedTask.data.difficulty); setAnswerKind(selectedTask.data.answerKind); }, [selectedTask.data]);
  const refreshAfterSave = (message: string) => { tasks.refetch(); toast.success(message); clearForm(); };
  const createTask = trpc.school.admin.createTask.useMutation({ onSuccess: () => refreshAfterSave("Задача добавлена в контентный список") });
  const updateTask = trpc.school.admin.updateTask.useMutation({ onSuccess: () => refreshAfterSave("Изменения сохранены") });
  const pending = createTask.isPending || updateTask.isPending;
  const submit = (event: React.FormEvent) => { event.preventDefault(); const payload = { title, slug: slug || slugify(title), statementMarkdown: statement, solutionMarkdown: solution, correctAnswer: answerKind === "manual" ? undefined : correctAnswer, answerKind, topicSlug: topic || defaultTopic, kimNumber: kim || defaultKim, difficulty, status }; if (editId) updateTask.mutate({ ...payload, taskId: editId }); else createTask.mutate(payload); };
  if (!loading && !isAdmin) return <div className="min-h-screen bg-[#0b0b0d] text-[#f5f0e9]"><PlatformHeader /><main className="container py-16"><section className="mx-auto max-w-2xl rounded-[28px] border border-[#ff5b14]/35 bg-[#ff5b14]/8 p-8"><p className="text-xs font-extrabold uppercase tracking-[.16em] text-[#ff7a35]">Нет доступа</p><h1 className="mt-4 text-3xl font-bold">Редактор доступен администратору.</h1><p className="mt-4 leading-7 text-[#aaa7ae]">Этот раздел отделён от личного кабинета ученика и репетитора.</p></section></main></div>;
  return <div className="min-h-screen bg-[#0b0b0d] text-[#f5f0e9]"><PlatformHeader /><main className="container py-10 sm:py-14"><section className="relative overflow-hidden rounded-[28px] border border-white/10 bg-[#151518] p-6 sm:p-8"><div className="pointer-events-none absolute -right-20 -top-20 h-64 w-64 rounded-full bg-[#ff5b14]/15 blur-3xl" /><div className="relative flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[#ff7a35]">Контентный слой</p><h1 className="mt-4 text-4xl font-bold tracking-[-.055em] sm:text-5xl">Редактор заданий</h1><p className="mt-4 max-w-xl text-sm leading-6 text-[#aaa7ae]">Создавайте авторские задачи, проверяйте разметку и управляйте статусом публикации.</p></div><div className="hidden items-center gap-2 rounded-xl border border-[#ff5b14]/30 bg-[#ff5b14]/10 px-4 py-3 text-sm font-bold text-[#ff8b4b] sm:flex"><ShieldCheck className="h-4 w-4" /> Редакторская зона</div></div></section><div className="mt-7 grid gap-5 xl:grid-cols-[1.08fr_.92fr]"><section className={panel}><div className="flex items-center justify-between gap-4"><div className="flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-lg bg-[#ff5b14] text-[#101014]"><FilePlus2 className="h-4 w-4" /></span><h2 className="text-lg font-bold">{editId ? "Редактирование задачи" : "Новая задача"}</h2></div>{editId ? <Button type="button" variant="ghost" size="sm" onClick={clearForm} className="gap-2 text-[#ff8b4b] hover:bg-[#ff5b14]/10 hover:text-[#ff9a61]"><Plus className="h-4 w-4" /> Новая</Button> : null}</div>{selectedTask.isLoading ? <div className="grid min-h-64 place-items-center"><Loader2 className="h-6 w-6 animate-spin text-[#ff5b14]" /></div> : <form className="mt-6 space-y-4" onSubmit={submit}><div className="grid gap-4 sm:grid-cols-2"><div><Label htmlFor="admin-title" className="text-[#c7c3ca]">Название</Label><Input id="admin-title" value={title} onChange={event => setTitle(event.target.value)} required className="mt-2 h-10" /></div><div><Label htmlFor="admin-slug" className="text-[#c7c3ca]">URL-идентификатор</Label><Input id="admin-slug" value={slug} onChange={event => setSlug(event.target.value)} placeholder="skidka-20" className="mt-2 h-10" /></div></div><div className="grid gap-4 sm:grid-cols-2"><div><Label className="text-[#c7c3ca]">Тема</Label><select value={topic || defaultTopic} onChange={event => setTopic(event.target.value)} className="mt-2 flex h-10 w-full rounded-lg px-3 text-sm">{options.data?.topics.map(item => <option key={item.slug} value={item.slug}>{item.title}</option>)}</select></div><div><Label className="text-[#c7c3ca]">Номер КИМ</Label><select value={kim || defaultKim} onChange={event => setKim(event.target.value)} className="mt-2 flex h-10 w-full rounded-lg px-3 text-sm">{options.data?.taskTypes.map(item => <option key={item.kimNumber} value={item.kimNumber}>{item.kimNumber} · {item.title}</option>)}</select></div></div><div className="grid gap-4 sm:grid-cols-3"><div><Label className="text-[#c7c3ca]">Сложность</Label><select value={difficulty} onChange={event => setDifficulty(event.target.value as Difficulty)} className="mt-2 flex h-10 w-full rounded-lg px-3 text-sm"><option value="basic">Базовый</option><option value="standard">Стандарт</option><option value="advanced">Повышенный</option></select></div><div><Label className="text-[#c7c3ca]">Ответ</Label><select value={answerKind} onChange={event => setAnswerKind(event.target.value as AnswerKind)} className="mt-2 flex h-10 w-full rounded-lg px-3 text-sm"><option value="short_integer">Целое число</option><option value="short_decimal">Десятичное число</option><option value="short_text">Текст</option><option value="manual">Ручная проверка</option></select></div><div><Label className="text-[#c7c3ca]">Статус</Label><select value={status} onChange={event => setStatus(event.target.value as Status)} className="mt-2 flex h-10 w-full rounded-lg px-3 text-sm"><option value="draft">Черновик</option><option value="review">На проверке</option><option value="published">Опубликовать</option></select></div></div><div><Label htmlFor="admin-statement" className="text-[#c7c3ca]">Условие</Label><Textarea id="admin-statement" value={statement} onChange={event => setStatement(event.target.value)} className="mt-2 min-h-28" required /></div>{answerKind !== "manual" ? <div><Label htmlFor="admin-answer" className="text-[#c7c3ca]">Правильный ответ для Части 1</Label><Input id="admin-answer" value={correctAnswer} onChange={event => setCorrectAnswer(event.target.value)} required className="mt-2 h-10" /></div> : null}<div><Label htmlFor="admin-solution" className="text-[#c7c3ca]">Разбор</Label><Textarea id="admin-solution" value={solution} onChange={event => setSolution(event.target.value)} className="mt-2 min-h-32" required /></div><Button type="submit" disabled={pending || options.isLoading} className={orangeButton}>{editId ? "Сохранить изменения" : status === "published" ? "Опубликовать" : "Сохранить в черновики"}</Button></form>}</section><section className={panel}><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">Контентный список</h2><p className="mt-2 text-sm text-[#aaa7ae]">Откройте запись, чтобы продолжить редактирование.</p></div><span className="font-['Space_Grotesk'] text-lg font-bold text-[#ff7a35]">{tasks.data?.length ?? 0}</span></div><div className="mt-5 max-h-[740px] space-y-2 overflow-y-auto pr-1">{tasks.isLoading ? <div className="grid min-h-32 place-items-center"><Loader2 className="h-6 w-6 animate-spin text-[#ff5b14]" /></div> : tasks.data?.map(task => <button type="button" key={task.id} onClick={() => setEditId(task.id)} className={`w-full rounded-xl border p-4 text-left transition ${editId === task.id ? "border-[#ff5b14]/65 bg-[#ff5b14]/8" : "border-white/8 bg-[#101012] hover:border-white/20 hover:bg-[#1a1a1e]"}`}><div className="flex flex-wrap gap-2"><Badge className="border-0 bg-[#ff5b14]/14 text-[#ff8b4b] hover:bg-[#ff5b14]/14">КИМ {task.kimNumber}</Badge><Badge variant="outline" className="border-white/12 text-[#b7b3ba]">{task.topicTitle}</Badge><Badge variant="outline" className={task.status === "published" ? "border-emerald-400/25 text-emerald-300" : task.status === "review" ? "border-[#ff5b14]/30 text-[#ff8b4b]" : "border-white/12 text-[#89868d]"}>{task.status === "published" ? "Опубликовано" : task.status === "review" ? "На проверке" : "Черновик"}</Badge></div><div className="mt-3 flex items-center justify-between gap-3"><div><p className="font-extrabold text-[#f5f0e9]">{task.title}</p><p className="mt-1 text-xs text-[#89868d]">/{task.slug}</p></div><Pencil className="h-4 w-4 text-[#ff7a35]" /></div></button>)}</div></section></div></main></div>;
}

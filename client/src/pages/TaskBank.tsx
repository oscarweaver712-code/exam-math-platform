import { PlatformHeader } from "@/components/PlatformHeader";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, ExternalLink, FileImage, Filter, Loader2, RotateCcw, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

const sourceLabel = { author: "Авторская задача Школы 911", fipi: "Составлено на основе ФИПИ", partner: "Внешний проверяемый источник" } as const;
const difficultyLabel = { basic: "Базовый", standard: "Стандарт", advanced: "Повышенный" } as const;

function FilterPill({ active, children, onClick }: { active: boolean; children: React.ReactNode; onClick: () => void }) {
  return <button type="button" onClick={onClick} className={`rounded-lg border px-3 py-2 text-xs font-extrabold transition ${active ? "border-[#ff5b14] bg-[#ff5b14] text-[#101014]" : "border-white/10 bg-white/5 text-[#aba7ae] hover:border-white/25 hover:bg-white/9 hover:text-white"}`}>{children}</button>;
}

export default function TaskBank() {
  const [topicSlug, setTopicSlug] = useState<string | undefined>(); const [difficulty, setDifficulty] = useState<"basic" | "standard" | "advanced" | undefined>(); const [part, setPart] = useState<"part1" | "part2" | undefined>(); const [kimNumber, setKimNumber] = useState<string | undefined>();
  const overview = trpc.publicBank.overview.useQuery(); const tasks = trpc.publicBank.listTasks.useQuery({ topicSlug, difficulty, part, kimNumber });
  const reset = () => { setTopicSlug(undefined); setDifficulty(undefined); setPart(undefined); setKimNumber(undefined); };
  return <div className="theme-page min-h-screen"><PlatformHeader /><main className="container py-10 sm:py-14"><section className="grid gap-7 border-b border-white/9 pb-9 lg:grid-cols-[minmax(0,1fr)_390px] lg:items-end"><div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[#ff7a35]">ОГЭ 2026 · математика</p><h1 className="mt-4 text-4xl font-bold tracking-[-.06em] sm:text-6xl">Банк заданий.<br /><span className="text-[#ff5b14]">Без лишнего.</span></h1><p className="theme-muted mt-5 max-w-2xl text-base leading-7">Выберите номер задания или тему. В каждой карточке сразу видны место в экзамене, происхождение материала и наличие проверенной схемы.</p></div><div className="theme-surface rounded-2xl border p-5"><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-[#77747b]">Сейчас доступно</p><div className="mt-2 flex items-end gap-3"><strong className="font-['Space_Grotesk'] text-5xl leading-none text-[#ff5b14]">{overview.data?.taskCount ?? tasks.data?.length ?? 0}</strong><span className="theme-muted pb-1 text-sm font-bold">проверенных тренировочных задач</span></div><p className="theme-muted mt-3 text-xs leading-5">Банк расширяется редакционно: черновики, архив и спорные записи не попадают в публичную выдачу.</p></div></section>
    <section className="theme-surface mt-7 rounded-2xl border p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2 text-sm font-extrabold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-[#ff5b14] text-[#101014]"><Filter className="h-4 w-4" /></span>Фильтры банка</div><Button variant="ghost" size="sm" onClick={reset} className="gap-2 text-[#ff8b4b] hover:bg-[#ff5b14]/10 hover:text-[#ffb187]"><RotateCcw className="h-3.5 w-3.5" />Сбросить</Button></div><div className="mt-5 grid gap-5"><div><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-[#77747b]">Номер задания ОГЭ</p><div className="mt-2 flex flex-wrap gap-2"><FilterPill active={!kimNumber} onClick={() => setKimNumber(undefined)}>Все номера</FilterPill>{overview.data?.taskTypes.map(item => <FilterPill key={item.kimNumber} active={kimNumber === item.kimNumber} onClick={() => setKimNumber(kimNumber === item.kimNumber ? undefined : item.kimNumber)}>№ {item.kimNumber}</FilterPill>)}</div></div><div className="grid gap-5 lg:grid-cols-3"><div><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-[#77747b]">Тема</p><div className="mt-2 flex flex-wrap gap-2"><FilterPill active={!topicSlug} onClick={() => setTopicSlug(undefined)}>Все темы</FilterPill>{overview.data?.topics.map(topic => <FilterPill key={topic.slug} active={topicSlug === topic.slug} onClick={() => setTopicSlug(topicSlug === topic.slug ? undefined : topic.slug)}>{topic.title}</FilterPill>)}</div></div><div><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-[#77747b]">Часть</p><div className="mt-2 flex flex-wrap gap-2"><FilterPill active={!part} onClick={() => setPart(undefined)}>Все</FilterPill><FilterPill active={part === "part1"} onClick={() => setPart(part === "part1" ? undefined : "part1")}>Часть 1</FilterPill><FilterPill active={part === "part2"} onClick={() => setPart(part === "part2" ? undefined : "part2")}>Часть 2</FilterPill></div></div><div><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-[#77747b]">Сложность</p><div className="mt-2 flex flex-wrap gap-2">{(Object.keys(difficultyLabel) as Array<keyof typeof difficultyLabel>).map(level => <FilterPill key={level} active={difficulty === level} onClick={() => setDifficulty(difficulty === level ? undefined : level)}>{difficultyLabel[level]}</FilterPill>)}</div></div></div></div></section>
    <section className="mt-8"><div className="mb-4 flex items-center justify-between"><p className="text-sm font-bold"><span className="mr-2 font-['Space_Grotesk'] text-2xl text-[#ff5b14]">{tasks.data?.length ?? 0}</span>заданий по выбранным условиям</p><p className="theme-muted text-xs">Показываются только опубликованные записи.</p></div>{tasks.isLoading ? <div className="grid min-h-52 place-items-center"><Loader2 className="h-7 w-7 animate-spin text-[#ff5b14]" /></div> : tasks.data?.length ? <div className="space-y-3">{tasks.data.map(task => <article key={task.id} className="group rounded-2xl border border-white/9 bg-white/3 p-5 transition hover:border-[#ff5b14]/65 hover:bg-[#ff5b14]/5 sm:p-6"><Link href={`/bank/${task.slug}`} className="block"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><Badge className="border-0 bg-[#ff5b14] text-[#101014] hover:bg-[#ff5b14]">Задание № {task.kimNumber}</Badge><Badge variant="outline" className="border-white/12 text-[#aaa7ae]">Часть {task.part === "part1" ? "1" : "2"}</Badge><Badge variant="outline" className="border-white/12 text-[#aaa7ae]">{task.topicTitle}</Badge><Badge variant="outline" className="border-emerald-400/25 bg-emerald-400/8 text-emerald-200">{task.status === "published" ? "Опубликовано" : task.status}</Badge>{task.hasReviewedVisual ? <Badge className="gap-1 border-0 bg-sky-400/12 text-sky-200 hover:bg-sky-400/12"><FileImage className="h-3 w-3" />Есть схема</Badge> : null}</div><h2 className="mt-4 text-xl font-bold tracking-[-.03em] text-[#f5f0e9]">{task.title}</h2><p className="theme-muted mt-2 line-clamp-2 max-w-3xl text-sm leading-6">{task.statementMarkdown}</p></div><span className="inline-flex shrink-0 items-center gap-2 text-sm font-extrabold text-[#ff7a35]">Открыть<ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1" /></span></div></Link><div className="mt-4 flex flex-wrap items-center gap-x-3 gap-y-2 border-t border-white/8 pt-4 text-xs"><span className="inline-flex items-center gap-1 rounded-md bg-white/5 px-2 py-1 font-bold text-[#d7d1c9]"><ShieldCheck className="h-3.5 w-3.5 text-[#ff8b4b]" />{sourceLabel[task.sourceKind]}</span><span className="theme-muted">Источник: {task.sourceTitle || "название не указано"}</span>{task.sourceRecordId ? <span className="theme-muted">ID источника: {task.sourceRecordId}</span> : null}{task.sourceUrl ? <a href={task.sourceUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 font-bold text-[#ff8b4b] hover:text-[#ffb187]">Источник<ExternalLink className="h-3.5 w-3.5" /></a> : <span className="theme-muted">Внешняя ссылка не требуется</span>}</div></article>)}</div> : <div className="theme-surface rounded-2xl border border-dashed p-8"><p className="font-bold">По этим условиям задач пока нет.</p><p className="theme-muted mt-2 text-sm">Снимите часть фильтров или вернитесь к полному каталогу.</p><Button onClick={reset} className="mt-4 rounded-xl bg-[#ff5b14] font-bold text-[#101014] hover:bg-[#ff7a35]">Показать все задания</Button></div>}</section></main></div>;
}
